[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "db_scanner"
version = "0.1.0"
description = "Database scanner module for SwanVA"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["db_scanner*"]
