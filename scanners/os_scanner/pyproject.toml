[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "os_scanner"
version = "0.1.0"
description = "OS detection scanner module for SwanVA"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["os_scanner*"]
